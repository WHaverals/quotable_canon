# Pip manifest for quotable_canon. Prefer environment.yml (conda/mamba) for a
# reliable Bayesian stack; plain pip may struggle with PyMC/PyTensor on some platforms.
#
#   pip install -r requirements.txt

# Core data + viz (all notebooks)
polars==1.29.0
pandas==2.3.3
numpy==2.2.6
pyarrow==22.0.0
altair==5.5.0
vl-convert-python==1.9.0.post1

# Frequentist stats (notebooks 02–03)
statsmodels==0.14.4
patsy==1.0.1
scipy==1.15.2

# Bayesian modeling (notebooks 03–04)
bambi==0.17.2
pymc==5.27.0
arviz==0.23.1
pytensor==2.36.3
xarray==2025.12.0
h5netcdf==1.8.0
graphviz==0.21

# Notebook runtime
jupyterlab==4.4.1
ipykernel==6.29.5
ipython==9.1.0
matplotlib==3.10.1

# PPA utilities — not on PyPI; pin to release tag
git+https://github.com/Princeton-CDH/corppa.git@0.4#egg=corppa
